name: checks

on:
  pull_request:
    paths:
      - "**/*.go"
      - go.mod
      - go.sum
      - Makefile
      - licenses/**
      - scripts/licenses.sh
      - .github/workflows/checks.yml
  push:
    branches: [main]
    paths:
      - "**/*.go"
      - go.mod
      - go.sum
      - Makefile
      - licenses/**
      - scripts/licenses.sh
      - .github/workflows/checks.yml
  schedule:
    - cron: "23 9 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: [self-hosted, tailmix-vm]
    container:
      image: golang:1.27.0
    steps:
      - uses: actions/checkout@v6
      - run: make check
